                                                        # Simple Shell Project
This is a simple shell project to be used as the culmination of C   
programming language learning.  
The objectives are:  
**Code that passes Betty checks
**Write a UNIX comman line interpreter
**Code that handles command lines with arguments  
**Simple Shell Handle command lines with arguments
**Handle the PATH
**Fork must not be called if the command doesn’t exist
**Implement the exit built-in, that exits the shell
**Implement the env built-in, that prints the current environment

